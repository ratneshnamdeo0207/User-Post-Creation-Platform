<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <style>
        body {
            background-color: black;
            font-family: Arial, sans-serif;
            color: #f0f0f0;
            margin: 0;
            padding: 20px;
           
            flex-direction: column;
            align-items: center; /* Center horizontally */
        }

        a {
            color: #4CAF50;
            text-decoration: none;
            padding: 10px;
            display: inline-block;
            margin-bottom: 20px;
            transition: background-color 0.3s;
        }

        a:hover {
            background-color: #333;
            color: white;
        }


        form {
            max-width: 515px;
    margin: 0 auto;
    padding: 20px;
    background-color: #1d1d1d;
    border-radius: 10px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    padding-top: 5%;
    padding-bottom: 5%;
        }

        label {
            font-weight: bold;
            margin-bottom: 5px;
            color: #e0e0e0;
        }

        input[type="text"], textarea {
            width: 100%;
            padding: 10px;
            margin: 5px 0 15px 0;
            border: 1px solid #3b86b9;
            border-radius: 5px;
            background-color: #2a2a2a;
            color: #f0f0f0;
            box-sizing: border-box; /* Important for correct input sizing */
        }

        input[type="text"]:focus, textarea:focus {
            border-color: #4CAF50;
            outline: none;
        }

        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%; /* Full width */
            margin-top: 10px;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #45a049;
        }

        textarea {
            resize: none; /* Prevent resizing */
            height: 200px; /* Fixed height for the textarea */
        }
    </style>
</head>
<body>
    <a href="/posts">Back</a>
    <form action="/editpost/:id?_method=PATCH" method="post">
        <label for="name">Enter your name</label>
        <input type="text" value="<%= post.name %>" name="name" id="name">

        <label for="post">Enter your post</label>
        <textarea name="post" id="post" rows="20" placeholder="Enter your post here"><%= post.post %></textarea>

        <button type="submit">Submit</button>
    </form>
</body>
</html>
