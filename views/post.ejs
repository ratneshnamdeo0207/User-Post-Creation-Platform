<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
          body {
            font-family: 'Roboto', sans-serif;
            background-color: #121212;
            color: #6dbe2b;
            margin: 0;
            padding: 20px;

        }
        h2{
            text-align: center;;
        }
        #title {
            background-color: #1d54d2;
            text-align: center;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
            font-size: 2.5em;
            color: white;
        }

        div {
            background-color: #1f1f1f;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 15px;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        div:hover {
            transform: scale(1.02);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
        }

        div h3 {
            background-color: #3b86b9;
            margin-top: 0;
            border-radius: 5px;
            padding: 10px;
            color: white;
        }

        div p {
            background-color: #f9e84f;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            color: black;
        }

        a {
            display: inline-block;
            margin-right: 10px;
            text-decoration: none;
            color: white;
            padding: 8px 12px;
            border-radius: 5px;
            background-color: #333;
            transition: background-color 0.3s;
        }

        a:hover {
            background-color: #555;
        }

        .create-post {
            display: block;
            text-align: center;
            margin-top: 20px;
            padding: 15px;
            background-color: #007BFF;
            color: white;
            border-radius: 5px;
            text-decoration: none;
            font-size: 1.2em;
            transition: background-color 0.3s;
        }

        .create-post:hover {
            background-color: #0056b3;
        }
    </style> 
</head>
<body>

    <h2>Your Post</h2>
    
    <br><br>
    <a href="/posts">Back</a>
    <br>
    <br>
    <div>
        <h3>@<%= post.name %></h3>
        <p><%= post.post %></p>
       
        <a href="/editpost/<%= post.id %>">Edit</a>
        <!-- <button id="detail">see in detail</button>
        <button id="edit">Edit</button> -->
    </div>
   
</body>
</html>